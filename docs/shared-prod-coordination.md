# Shared Production Coordination — STUB (not the source of truth)

⚠️ **Do not edit this file, and do not let it grow back into a full copy.**

The shared Purchasing + Recipe production-coordination playbook has a **single
source of truth**, which lives in the **Purchasing repo**:

```
C:\Code\freytags-purchasing\docs\SHARED-PROD-COORDINATION.md
```

Both repos are on the same machine, so a Claude session in either repo can read
that canonical path directly.

**To propose a change** (e.g. after a recipe-side surgical rules change, or a new
recipe-owned collection): describe the change and hand the exact text to the
**Purchasing side**, which owns and commits the canonical file — exactly how the
canonical `firestore.rules` already works. One real file, zero drift.

Recipe-side implementation specifics (how *this* repo does rules changes, the
no-`firestore`-block `firebase.json`, functions-codebase rule, verified recovery
posture) live in this repo's `CLAUDE.md` → "Shared prod rules — coordination".
